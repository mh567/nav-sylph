# Navigation Page 服务器配置
# 复制此文件为 .env 并根据需要修改

# ===== 服务器配置 =====
# 监听地址 (默认: 127.0.0.1，仅本地访问)
# 设置为 0.0.0.0 允许外部访问
SERVER_HOST=127.0.0.1

# 监听端口 (默认: 4000)
SERVER_PORT=4000

# ===== HTTPS 配置 (可选) =====
# 是否启用 HTTPS (true/false)
HTTPS_ENABLED=false

# SSL/TLS 证书路径 (HTTPS_ENABLED=true 时必填)
HTTPS_KEY_PATH=
HTTPS_CERT_PATH=
HTTPS_CA_PATH=

# ===== 安全配置 =====
# 管理员密码文件路径 (默认: .admin-password.json)
ADMIN_PASSWORD_FILE=.admin-password.json

# ===== 路径配置 =====
# 书签数据文件 (默认: data.json)
DATA_FILE=data.json

# 日志目录 (默认: logs)
LOG_DIR=logs

# 图标路径
ICON_PATH=icon.svg
FAVICON_PATH=favicon.svg

# ===== WebDAV 远程备份 (可选) =====
# WebDAV 配置存储在 .webdav-config.json 中（密码加密存储）
# 通过管理面板配置，无需在此设置
# WEBDAV_CONFIG_FILE=.webdav-config.json
